import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Settings = ({ sleepRating, emotions, setSettings}) => {
  return (
    <View>
      <Text>Settings!</Text>
    </View>
  )
}

export default Settings;